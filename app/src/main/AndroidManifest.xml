<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myplacements"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Myplacements">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity
            android:name=".RegisterActivity1"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity
            android:name=".HomeActivity"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity
            android:name=".ContributeMaterialActivity"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity
            android:name=".AlumniContactActivity"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity
            android:name=".PlacementMaterialAdapter"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity
            android:name=".PlacementMaterialActivity"
            android:exported="false"/> <!-- Change to false if not needed externally -->

        <activity android:name=".ResumeTemplatesActivity"
            android:exported="false"/>

        <activity android:name=".ResumeTemplateAdapter"
            android:exported="false"/>

    </application>

</manifest>
